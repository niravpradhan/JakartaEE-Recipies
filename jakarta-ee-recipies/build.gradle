plugins {
    id 'war'
}

dependencies {
    providedCompile "jakarta.platform:jakarta.jakartaee-api:${jakartaee_api_version}"
    implementation "org.primefaces:primefaces:${primefaces_version}:jakarta"
}